'use strict';

angular.module('mean').controller('ListesController', ['$scope', 'Global', 'Menus', '$rootScope', '$http', 'Users',
    function($scope, Global, Menus, $rootScope, $http, Users) {
        $scope.global = Global;

         $scope.init = function() {
            Users.query({}, function(users) {
                $scope.users = users;
            });
        };




    }
]);